name: hexlet-check

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - name: Install
      - run: make install    
      - name: Run linter
      - run: make lint
